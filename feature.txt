This is a new feature
git add feature.txt -m "Add feature description"
# Clone the repository and navigate into the directory
git clone <REPOSITORY-URL>
cd GitHubCloneProject

# Add a README.md if it doesn't exist
echo "This is a project to practice Git workflows." > README.md

# Commit the initial README.md
git add README.md
git commit -m "Initial commit with README.md"

# Push the initial commit to your remote repository
git push origin main
# Create and switch to the new branch
git checkout -b feature-greeting

# Create and add content to a new file
echo "This is a new feature." > feature.txt

# Stage and commit the new file
git add feature.txt
git commit -m "Add feature description"
# Switch back to the main branch
git checkout main

# Modify the README.md file
echo "Modification in main branch." >> README.md

# Stage and commit the change
git add README.md
git commit -m "Modify README in main branch"
# Push the updated main branch to the remote repository
git push origin main

